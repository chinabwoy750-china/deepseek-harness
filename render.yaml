services:
  - type: web
    name: deepseek-harness
    runtime: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile --ignore-scripts && pnpm run build
    startCommand: pnpm dsh web --host 0.0.0.0 --port $PORT --no-open
    envVars:
      - key: NODE_ENV
        value: production
